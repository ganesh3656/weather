version: 2.1

jobs:
  run-script:
    docker:
      - image: cimg/python:3.8  # Use a Python Docker image
    steps:
      - checkout  # Checkout the code from the repository
      - run:
          name: Install dependencies
          command: |
            # Install dependencies if you have any; not needed for this simple script
            echo "No dependencies to install"
      - run:
          name: Run practice.py
          command: |
            python practice.py

workflows:
  version: 2
  run-script-workflow:
    jobs:
      - run-script
